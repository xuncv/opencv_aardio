import win.ui;
/*DSG{{*/
var winform = win.form(text="aardio form";right=759;bottom=469)
winform.add(
button={cls="button";text="Button";left=613;top=78;right=720;bottom=149;z=2};
plus={cls="plus";left=36;top=30;right=435;bottom=368;z=1}
)
/*}}*/

import cv2


winform.button.oncommand = function(id,event){
    winform.text = "open camera"
	camera = cv2.videoCapture(0)
	winform.text = "opened camera"
/*
	while(win.delay(1)){
		winform.text = tostring(time())
		m = camera.read();
		if !m.empty(){
			bmp,err = m.toBitmap()
			winform.plus.setForeground(bmp)
			winform.plus.redraw()
		}
		win.delay(100)
	
	}
*/
}

winform.show();
win.loopMessage();
return winform;